CREATE TABLE `package` (
  `number` varchar(20) DEFAULT NULL,
  `name` varchar(11),
  `phone` varchar(33),
  `weight`  varchar(11),
  `bookingTime` varchar(11),
  `status` varchar(33),
  PRIMARY KEY (`number`)
); 